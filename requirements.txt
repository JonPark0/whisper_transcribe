# Core dependencies
torch>=2.2.0
transformers>=4.52.0
librosa>=0.10.0
ffmpeg-python>=0.2.0
soundfile>=0.12.0
pydub>=0.25.0

# Enhancement with Gemini API
google-generativeai>=0.8.0
python-dotenv>=1.0.0

# Note: Flash Attention 2 and build tools (ninja, psutil, flash-attn) are optional
# Install with: pip install whisper-transcribe[flash-attn]
# Or: pip install -r requirements.txt && pip install flash-attn --no-build-isolation